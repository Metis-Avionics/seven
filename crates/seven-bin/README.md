# Seven end-to-end reference pipeline

Deterministic evidence→belief pipeline runnable as `cargo run -p seven-bin`.
Not for operational use (spec §21); exists to prove the deterministic core
end-to-end over the simulated lossy transport.
