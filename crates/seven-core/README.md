# Canonical deterministic aviation physical-state representation (Seven S01/S02)

Quaternion hemisphere rule, quantization policy, canonical byte encoding (postcard). Defines the domain types all other Seven crates consume.

Part of the Mētis Avionics/Seven workspace; see the root README for the system picture and `specs/` for the authoritative contract.
