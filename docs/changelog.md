# Changelog

This file is auto-generated by `prv-cli living update`.

## From living.toml

- : Single squashed commit for v0.1 (user decision) (``)
- : theMQL consumed from crates.io 0.1 (owner-published); vendoring abandoned (decision 0003) (``)
- : Canonical codec = postcard (36B vs 88B Borsh, bench decision 0001); FormatTag::Postcard already exists upstream (``)
- : Quaternion hemisphere rule: first-nonzero-component-positive, tie order w→x→y→z (Seven owns; no Mētis primitive defines it) (``)
- : prv-monte-carlo declared but its Simulator not used for aviation state (economic State coupling, decision 0002) (``)
- : Belief engine: deterministic linear-Gaussian conjugate for independent evidence; themql-estimation EKF composed for IMU-fused tracks (Phase 8 wiring) (``)
- : tokio pinned =1.52.4 (1.53.1 fails internally on rustc 1.98.1) (``)
- : HelixDB dev instance uses memory storage under podman (CLI-managed MinIO short-name fails rootless); disk persistence via --storage-uri for real deployments (``)
- : HelixDB endpoint defaults to IPv4 127.0.0.1:6969 — podman pasta forwarding is v4-only (``)
- : Workspace scaffolded; helix.toml moved to podman (``)
- : specs/ v0.1 landed before code (seven.toml + S01–S18 TOMLs) (``)
- : seven-core canonical pipeline + proptest idempotence (postcard, quantization) (``)
- : seven-evidence: blake3 EvidenceId, ed25519 signatures, provenance forwarding (invariants 5,6,7,10) (``)
- : seven-belief: deterministic linear-Gaussian updates, duplicate/conflict gates (8,9,11) (``)
- : seven-helix query suite + live podman integration (15) (``)
- : seven-replay double-pass fingerprint (14) (``)
- : seven-sim LossyChannel with observable fault logging (16) (``)
- : seven-mql Message projection roundtrip (12) (``)
- : seven-six AviationCachePolicy over thesix; anonymous mutation denied (``)
- : 40/40 nextest green, clippy -D warnings silent, release build green (``)
- : Codec bench: postcard 36B vs Borsh 88B (decision 0001 evidence) (``)
- : specs/VERIFICATION.md maps all 17 spec-§22 invariants to named tests (``)
- : Crate READMEs + publish metadata for all seven-* crates (``)
- spec_driven_v0_1:  (``)
